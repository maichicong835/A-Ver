name: A-Ver TSDR Targeted Record Canary

on:
  push:
    branches: [main]
    paths:
      - 'src/aver/tsdr_targeted_canary.py'
      - '.github/workflows/tsdr-targeted-canary.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: aver-tsdr-targeted-canary
  cancel-in-progress: false

env:
  EXTERNAL_SIDE_EFFECTS_AUTHORIZED: 'false'
  DAILY7_LIVE_GATE_AUTHORIZED: 'false'
  TM_DECISION_AUTHORIZED: 'false'

jobs:
  targeted-record:
    runs-on: ubuntu-24.04
    timeout-minutes: 6
    steps:
      - name: Checkout exact A-Ver commit
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 1
      - name: Verify bounded official-record locks
        shell: bash
        run: |
          set -euo pipefail
          test "$EXTERNAL_SIDE_EFFECTS_AUTHORIZED" = "false"
          test "$DAILY7_LIVE_GATE_AUTHORIZED" = "false"
          test "$TM_DECISION_AUTHORIZED" = "false"
          python3 - <<'PY'
          import json
          c=json.load(open('CURRENT.json'))
          assert c['status']=='PRODUCTION_APPROVED_SCOPED'
          assert c['current_reopen_scope']['new_candidate_or_market_discovery_forbidden'] is True
          assert c['current_reopen_scope']['daily7_live_gate_authorized'] is False
          print('AVER_TSDR_TARGETED_LOCK_PASS')
          PY
      - name: Prepare normal browser
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --disable-pip-version-check --quiet playwright==1.55.0
          browser="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          if [ -n "$browser" ]; then echo "AVER_BROWSER_EXECUTABLE=$browser" >> "$GITHUB_ENV"; else python3 -m playwright install chromium; fi
      - name: Bind two known records through official TSDR
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=src python3 src/aver/tsdr_targeted_canary.py
      - name: Upload immutable TSDR evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aver-tsdr-targeted-${{ github.run_id }}-${{ github.sha }}
          path: artifacts/tsdr-targeted/receipt.json
          if-no-files-found: error
          retention-days: 30
      - name: Assert targeted record pass
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          r=json.load(open('artifacts/tsdr-targeted/receipt.json'))
          assert r['phase_state']=='TSDR_TARGETED_RECORD_PASS'
          assert r['official_source'] is True
          assert r['known_identifiers_only'] is True
          assert r['candidate_breadth_expanded'] is False
          assert r['tm_decision_authorized'] is False
          assert r['daily7_live_gate_authorized'] is False
          assert all(x['state']=='PASS' for x in r['cases'])
          print('AVER_TSDR_TARGETED_RECORD_PASS')
          PY
